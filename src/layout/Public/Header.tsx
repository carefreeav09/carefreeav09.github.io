import React from "react";
import { Navbar } from "../../components";

const Header = () => {
  return (
    <Navbar
      title={"Carefreeav"}
      size="md"
      transparent
      spacing="container"
      navLists={[]}
    />
  );
};

export default Header;
